<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.form">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Actividades.ListaFormulariosActivity"
            android:label="@string/title_activity_lista_formularios"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Actividades.NavDrawerActivy"
            android:label="@string/title_activity_nav_drawer_activy"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Actividades.ForgotPassword" />
        <activity android:name=".Actividades.CreateUser" />
        <activity android:name=".Actividades.Formulario" />
        <activity android:name=".Actividades.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>